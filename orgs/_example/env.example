# Env var NAMES this org's boxes expect (no values). Values are rendered on-box into
# ~/workspace/<org>/.env by `cnc secrets <org> --sync`, read from the vault per bw.yml.
GITHUB_TOKEN=
ANTHROPIC_API_KEY=
# SLACK_BOT_TOKEN=
# LINEAR_API_KEY=
